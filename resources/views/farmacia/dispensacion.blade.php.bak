@extends('farmacia.layout')
@section('title', 'Dispensación')
@section('content')
<div class="p-6 bg-gray-50 min-h-screen">
    <h1 class="text-2xl font-bold text-gray-800 mb-6">Dispensar Medicamento</h1>

    <div class="max-w-2xl mx-auto bg-white p-8 rounded-xl shadow-sm border">
        <form action="{{ route('farmacia.dispense') }}" method="POST">
            @csrf
            
            <!-- BUSCADOR INTELIGENTE: MÉDICO -->
            <div class="mb-6">
                <label class="block text-sm font-medium text-gray-700 mb-2">Recetado por:</label>
                <input type="text" id="doctor-search" list="doctors-list" class="w-full border-gray-300 rounded-lg shadow-sm focus:ring-blue-500 focus:border-blue-500" placeholder="Escribe el nombre o rol del médico..." required>
                <input type="hidden" name="doctor_id" id="doctor-id" required>
                <datalist id="doctors-list">
                    @foreach($doctors as $doc)
                        <option value="{{ $doc->name }} - {{ $doc->role }}" data-id="{{ $doc->id }}">
                    @endforeach
                </datalist>
                <p class="text-xs text-gray-400 mt-1">Ejemplo: "CARLOS" o "Médico C"</p>
            </div>

            <!-- BUSCADOR INTELIGENTE: PACIENTE -->
            <div class="mb-6">
                <label class="block text-sm font-medium text-gray-700 mb-2">Paciente:</label>
                <input type="text" id="patient-search" list="patients-list" class="w-full border-gray-300 rounded-lg shadow-sm focus:ring-blue-500 focus:border-blue-500" placeholder="Escribe el nombre del paciente..." required>
                <input type="hidden" name="patient_id" id="patient-id" required>
                <datalist id="patients-list">
                    @foreach($patients as $p)
                        <option value="{{ $p->patient_name }} (Triage: {{ $p->triage_level }})" data-id="{{ $p->id }}">
                    @endforeach
                </datalist>
                <p class="text-xs text-gray-400 mt-1">Ejemplo: "Gabriela" o "Axel"</p>
            </div>

            <!-- BUSCADOR INTELIGENTE: MEDICAMENTO -->
            <div class="mb-6">
                <label class="block text-sm font-medium text-gray-700 mb-2">Medicamento:</label>
                <input type="text" id="med-search" list="meds-list" class="w-full border-gray-300 rounded-lg shadow-sm focus:ring-blue-500 focus:border-blue-500" placeholder="Escribe el nombre del medicamento..." required>
                <input type="hidden" name="medication_id" id="med-id" required>
                <datalist id="meds-list">
                    @foreach($medications as $m)
                        <option value="{{ $m->name }} (Stock: {{ $m->stock }} | Nivel: {{ $m->required_level }})" data-id="{{ $m->id }}">
                    @endforeach
                </datalist>
                <p class="text-xs text-gray-400 mt-1">Ejemplo: "Acido" o "Paracetamol"</p>
            </div>

            <!-- CANTIDAD -->
            <div class="mb-8">
                <label class="block text-sm font-medium text-gray-700 mb-2">Cantidad:</label>
                <input type="number" name="quantity" min="1" value="1" class="w-32 border-gray-300 rounded-lg shadow-sm focus:ring-blue-500 focus:border-blue-500" required>
            </div>

            <!-- BOTÓN -->
            <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg shadow transition w-full">
                <i class="fas fa-prescription-bottle-medical mr-2"></i> Dispensar Receta
            </button>
        </form>
    </div>

    <!-- ÚLTIMAS DISPENSACIONES -->
    <div class="max-w-2xl mx-auto mt-10">
        <h3 class="text-lg font-bold text-gray-700 mb-4">Últimas Dispensaciones</h3>
        <div class="bg-white rounded-xl shadow-sm border overflow-hidden">
            <table class="w-full text-sm text-left">
                <thead class="bg-gray-50 text-gray-600 uppercase">
                    <tr>
                        <th class="p-4">Paciente</th>
                        <th class="p-4">Medicamento</th>
                        <th class="p-4">Fecha</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach($recent as $r)
                    <tr class="border-t hover:bg-gray-50">
                        <td class="p-4">{{ $r->patient_name }}</td>
                        <td class="p-4">{{ $r->medication_name }} (x{{ $r->quantity }})</td>
                        <td class="p-4 text-gray-500">{{ $r->created_at->diffForHumans() }}</td>
                    </tr>
                    @endforeach
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- LÓGICA DEL BUSCADOR: Pasa el ID al input oculto cuando seleccionas una opción -->
<script>
    // Arreglar un bug visual de algunos navegadores con datalist
    document.querySelectorAll('input[list]').forEach(input => {
        input.addEventListener('change', function() {
            const datalistId = this.getAttribute('list');
            const datalist = document.getElementById(datalistId);
            const hiddenInputId = this.id.replace('-search', '-id');
            const hiddenInput = document.getElementById(hiddenInputId);
            
            if (datalist && hiddenInput) {
                const options = datalist.options;
                for (let i = 0; i < options.length; i++) {
                    if (options[i].value === this.value) {
                        hiddenInput.value = options[i].getAttribute('data-id');
                        break;
                    }
                }
            }
        });
    });
</script>
@endsection
